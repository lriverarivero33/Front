<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Local</title>
</head>
<body>
  <% if (!user) { %>
  <a href="/signin">Sign in</a>
  <a href="/signup">Sign up</a>
  <% } else { %>
  <a href="/logout">Logout</a>
  <a href="/profile">Profile</a>
  <% } %>

  <%if (signinMessage) {%>
    <p><%= signinMessage %></p>
  <% } %>
  <%if (signupMessage) {%>
    <p><%= signupMessage %></p>
  <% } %>

  <%- body %>
</body>
</html>
